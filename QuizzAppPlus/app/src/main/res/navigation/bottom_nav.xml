<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/bottom_nav"
    app:startDestination="@id/quizz">

    <fragment android:id="@+id/quizz"
        android:label="Home"
        android:name="com.example.quizzapp.ui.quizz.QuizzFragment"
        tools:layout="@layout/fragment_quizz"/>
    <fragment android:id="@+id/history"
        android:label="History"
        android:name="com.example.quizzapp.history.HistoryFragment"
        tools:layout="@layout/fragment_history"/>

</navigation>